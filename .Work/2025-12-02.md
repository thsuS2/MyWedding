# 작업 일지 - 2025년 12월 2일

## 📋 작업 개요
프로젝트 코드 검토 및 Phase 1 리팩토링 작업 완료

---

## ✅ 완료된 작업

### 1. 상수 파일 생성 (`src/constants/wedding.js`)
- 결혼식 정보를 중앙에서 관리할 수 있도록 상수 파일 생성
- 포함된 정보:
  - 결혼식 날짜 정보 (2026년 5월 30일)
  - 커플 정보 (신부/신랑 이름, 부모님 성함)
  - 예식장 정보 (이름, 주소, 교통 정보)
  - 계좌번호 정보
  - 카카오톡 공유 정보
  - 메타 태그 정보
- 날짜 포맷팅 함수 제공 (`getFormattedDate`, `getFormattedDateTime`, `getFormattedDateWithWeekday`)

### 2. 날짜 정보 일괄 수정
- 모든 컴포넌트에서 날짜를 **2026년 5월 30일**로 통일
- 수정된 파일:
  - `src/components/IntroSection.jsx`
  - `src/components/MainSection.jsx`
  - `index.html`
  - `src/utils/kakaoShare.js` (상수 파일 사용으로 자동 반영)

### 3. 코드 리팩토링
- **MapSection.jsx**: 중복된 클립보드 복사 로직 제거, `clipboard.js` 유틸 사용
- **모든 컴포넌트**: 하드코딩된 값들을 상수 파일로 이동
  - `IntroSection.jsx`: 커플 이름, 날짜
  - `MainSection.jsx`: 부모님 성함, 예식장 정보
  - `MapSection.jsx`: 예식장 정보, 계좌번호, 교통 정보
  - `kakaoShare.js`: 공유 정보

### 4. 컴포넌트별 변경 사항

#### IntroSection.jsx
- 상수 파일에서 커플 이름과 날짜 가져오기
- `COUPLE.bride.name`, `COUPLE.groom.name` 사용
- `getFormattedDateTime()` 함수 사용

#### MainSection.jsx
- 부모님 성함을 상수 파일에서 가져오기
- 예식장 정보를 상수 파일에서 가져오기
- `getFormattedDateWithWeekday()` 함수 사용

#### MapSection.jsx
- `clipboard.js`의 `copyAccount` 함수 사용
- 예식장 정보, 계좌번호를 상수 파일에서 가져오기
- 교통 정보를 상수 파일에서 가져오기

#### kakaoShare.js
- 카카오톡 공유 정보를 상수 파일에서 가져오기
- 날짜 정보 자동 반영

#### index.html
- 메타 태그의 날짜 정보를 2026년 5월 30일로 수정

---

## 📝 다음 단계 (Phase 2)

### 실제 데이터 입력 필요 항목
다음 정보들을 `src/constants/wedding.js` 파일에서 실제 데이터로 교체해야 합니다:

1. **커플 정보**
   - [ ] 신부 부모님 성함 (`COUPLE.bride.parents.father`, `mother`)
   - [ ] 신랑 부모님 성함 (`COUPLE.groom.parents.father`, `mother`)
   - [ ] 실제 성함 확인 (`COUPLE.bride.fullName`, `COUPLE.groom.fullName`)

2. **예식장 정보**
   - [ ] 예식장 이름 (`VENUE.name`)
   - [ ] 층수 (`VENUE.floor`)
   - [ ] 홀명 (`VENUE.hall`)
   - [ ] 정확한 주소 (`VENUE.address`)
   - [ ] 지하철 정보 (`VENUE.transportation.subway`)
   - [ ] 버스 정보 (`VENUE.transportation.bus`)
   - [ ] 주차 정보 (`VENUE.transportation.parking`)

3. **계좌번호**
   - [ ] 신랑 계좌 (은행명, 계좌번호)
   - [ ] 신부 계좌 (은행명, 계좌번호)

4. **날짜 정보**
   - [ ] 실제 시간 (`WEDDING_DATE.time` - 예: '오후 2시')
   - [ ] 요일 확인 (`WEDDING_DATE.weekday` - 2026년 5월 30일은 토요일)

5. **배포 정보**
   - [ ] 실제 배포 URL (`KAKAO_SHARE.imageUrl`, `META.ogUrl`)

---

## 🔍 코드 품질

### 린터 검사
- ✅ 모든 수정된 파일 린터 오류 없음
- ✅ ESLint 규칙 준수

### 코드 구조 개선
- ✅ 중복 코드 제거 (클립보드 복사 로직)
- ✅ 하드코딩된 값 제거
- ✅ 단일 책임 원칙 준수 (상수 파일로 분리)

---

## 📌 참고 사항

1. **상수 파일 위치**: `src/constants/wedding.js`
   - 모든 결혼식 관련 정보를 한 곳에서 관리
   - 실제 데이터 입력 시 이 파일만 수정하면 됨

2. **날짜 포맷팅 함수**
   - `getFormattedDate()`: "2026년 5월 30일"
   - `getFormattedDateTime()`: "2026년 5월 30일 오후"
   - `getFormattedDateWithWeekday()`: "2026년 5월 30일 (토요일) 오후"

3. **다음 작업 시 주의사항**
   - 실제 데이터 입력 전에 상수 파일의 주석을 확인할 것
   - 모든 플레이스홀더(○○)를 실제 값으로 교체할 것
   - 배포 URL은 GitHub Pages 설정 후 업데이트할 것

---

## 🎯 완료율

- Phase 1: ✅ 100% 완료
- Phase 2: ⏳ 대기 중 (실제 데이터 입력 필요)
- Phase 3: ✅ Vercel 배포 준비 완료

---

## 🚀 Vercel 배포 준비 작업 (추가)

### 완료된 작업
1. **vite.config.js 수정**
   - `base: '/MyWedding/'` → `base: '/'` 변경
   - Vercel 루트 도메인 사용 가능하도록 설정

2. **vercel.json 생성**
   - SPA 라우팅 설정 (rewrites)
   - 보안 헤더 추가 (X-Content-Type-Options, X-Frame-Options, X-XSS-Protection)

3. **배포 가이드 작성**
   - `VERCEL_DEPLOY.md` 파일 생성
   - 단계별 배포 가이드 포함
   - 환경 변수 설정 방법
   - 문제 해결 가이드

### 다음 단계 (사용자 작업)
1. Vercel 계정 생성 및 GitHub 연동
2. 프로젝트 배포 (Vercel 대시보드에서)
3. 환경 변수 설정 (Kakao, Supabase 키)
4. 배포 확인 및 테스트

### 배포 장점
- ✅ 더 간단한 URL (루트 도메인 사용)
- ✅ 자동 배포 (GitHub push 시)
- ✅ 프리뷰 배포 (PR마다)
- ✅ 환경 변수 관리 쉬움
- ✅ 빠른 배포 속도

---

**작성자**: AI Assistant  
**작성일**: 2025년 12월 2일  
**업데이트**: Vercel 배포 준비 완료

