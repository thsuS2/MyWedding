# 프로젝트 전체 검토 및 디자인 점검 - 2026-02-18

## 1. 요약

- **프로젝트**: 지수 🫶 유신 모바일 청첩장 (React + Vite)
- **검토 범위**: 전체 구조, 데이터, 디자인 시스템, UI 일관성, 이전 리뷰 반영 여부
- **종합 평가**: 완성도 높음. 데이터·메타는 실제 값 반영됨. 디자인은 일부 타이포/radius 통일만 보완하면 됨.

---

## 2. 프로젝트 구조

### 2.1 기술 스택
- **React 19** + **Vite 7**
- **Supabase**: 메시지 보드, RSVP
- **react-icons** (Pi 계열)
- 배포: Vercel (og:url 기준), GitHub Actions/gh-pages 설정 있음

### 2.2 폴더 구조 (잘 정리됨)
- `src/components/`: intro, main, date, map, gallary, message, account, 공통(Navigation, Footer, RSVPModal, Toast)
- `src/constants/wedding.js`: 결혼식/커플/예식장/계좌/카카오 공유 등 단일 소스
- `src/styles/`: globals.css(변수·리셋·공통), typography.css
- `src/contexts/`: ToastContext
- `src/utils/`: scrollAnimation, kakaoShare
- `src/lib/`: supabase

### 2.3 데이터·메타 현황
- **wedding.js**: 신랑/신부·부모·전화번호·계좌·예식장 정보 실제 데이터로 채워져 있음.
- **index.html**: `og:url` = `https://jisooyusinwedding.vercel.app/` 로 배포 URL 반영됨.
- **Calendar.css**: 이전 UI_REVIEW에 있던 `padding: 20pxㄴ` 오타는 이미 수정됨(현재 `20px`).

---

## 3. 디자인 시스템 점검

### 3.1 색상·타이포·간격 (globals.css + typography.css)
- **색상**: `--bg-white`, `--bg-ivory`, `--point-pink`, `--point-pink-light/dark`, `--text-dark/gray/light` 일관 사용.
- **폰트**: Playfair Display(영문 타이틀), MaruBuri/Pretendard(본문·한글 타이틀), Google/CDN 로드.
- **타이포 클래스**: `text-display`, `text-heading-large/medium/small`, `text-body`, `text-body-gray`, `text-caption` 등 체계적 정의.
- **간격**: `--spacing-xs` ~ `--spacing-xl` 사용. 섹션은 `padding: var(--spacing-xl) 0` 로 통일.
- **그림자**: `--shadow-sm/md/lg` 사용.

### 3.2 버튼·카드 border-radius (현재 상태)
- **8px**: 대부분 버튼, 캘린더 셀, Footer 버튼, MapSection 요소 등 → 메인 버튼/작은 요소.
- **12px**: MessageCard 관계 뱃지, Button.css variants, Toast, RSVPModal 필드·버튼, AccountCard.
- **16px**: 메시지 카드, DDayCounter, AccountSection 카드, 갤러리, MapSection 주소 박스 등 → 카드/블록.

**정리**: “버튼 8px, 카드 16px”로 대부분 맞고, 12px는 중간 요소(뱃지·입력 등)에 쓰여 논리적. UI_REVIEW에서 제안한 “btn-rsvp 12px→8px”, “account-item 12px→16px” 등은 선택 적용 가능한 수준.

---

## 4. UI_REVIEW·PROJECT_REVIEW 대비 현황

### 4.1 이미 반영된 것
- **og:url**: 실제 배포 URL로 수정됨.
- **Calendar.css 오타**: `20pxㄴ` → `20px` 수정됨.
- **Toast**: 커스텀 Toast + ToastContext 사용. RSVP/메시지 성공 등은 alert 대신 토스트 사용 중.
- **wedding.js**: 연락처·계좌 등 실제 데이터 반영.

### 4.2 아직 적용 가능한 것 (우선순위순)

#### (1) MainSection 타이포 강조 (UI_REVIEW 권장)
- **현재**: `main-date-time`, `couple-name` 등이 `text-body-gray`.
- **권장**:  
  - 날짜/시간: `text-heading-medium`  
  - 커플 이름: `text-heading-large`  
→ 메인 문구·이름이 더 잘 드러남.

#### (2) border-radius 통일 (선택)
- `.btn-rsvp` 12px → 8px (다른 버튼과 통일)
- `.message-relationship` 12px → 8px
- `.account-item` 12px → 16px (카드와 통일)

#### (3) 카카오 공유 실패 시 alert → Toast
- **현재**: `src/utils/kakaoShare.js`에서 실패 시 `alert()` 사용.
- **개선**: ToastContext를 사용해 토스트로 에러 메시지 표시 (전역 Toast 사용 가능하다면).

#### (4) 디버그 로그 (PROJECT_REVIEW)
- MapSection, useSupabase 등 `console.log` 프로덕션에서 제거 또는 `import.meta.env.DEV` 조건부 로깅.

---

## 5. 디자인 상세 체크

### 5.1 IntroSection
- `text-display`(커플 이름), `text-heading-medium`(결혼 합니다) 사용 적절.
- PetalAnimation, 배경·텍스트 페이드 인/아웃 구조 명확.

### 5.2 MainSection
- 인용문(`text-quote`), 초대문(`text-body-gray`) 구분 좋음.
- **개선점**: 날짜·커플 이름에 위 (1)번 타이포 적용 시 계층 강화.

### 5.3 DateSection
- DDayCounter, Calendar 컴포넌트 분리. `border-radius: 8px` 일관.

### 5.4 MapSection
- 주소·교통·RSVP 버튼 구성. point-pink 계열 버튼 사용.

### 5.5 GallerySection
- LazyImage, 모달 네비게이션. 16px 카드, 8px 버튼 사용.

### 5.6 MessageBoard / MessageCard
- 관계 필터, 카드 16px, 관계 뱃지 12px.

### 5.7 AccountSection
- 토글(세그먼트 슬라이더 + 탭 언더라인) 구조. 2026-01-28 작업 반영됨.
- 계좌 카드 border-radius 12px(AccountCard) / 16px(섹션 카드) 혼용 → 통일 시 16px 권장.

### 5.8 Footer
- 카카오 공유 버튼, footer 메시지. `text-body-gray` 사용.

### 5.9 공통
- Navigation: 플로팅 메시지 버튼.
- RSVPModal: 폼 검증·제출·토스트 연동.
- Toast: success/error/info/warning 타입, 아이콘·닫기 지원.

---

## 6. 반응형·접근성·성능

- **반응형**: `max-width: 480px` 미디어쿼리, `--max-width: 480px`, 모바일 우선 레이아웃.
- **접근성**: `aria-label`, 시맨틱 section/footer, 모달 포커스 관리는 추가 개선 여지 있음(Escape, 포커스 트랩).
- **성능**: LazyImage, 스크롤 애니메이션, Vite 청크 분리.

---

## 7. 정리 및 권장 액션

| 구분 | 상태 | 비고 |
|------|------|------|
| 데이터·메타 | ✅ | wedding.js, og:url 실제 값 반영 |
| 디자인 시스템 | ✅ | 색상·타이포·간격·그림자 일관 |
| border-radius | ⚠️ | 전반적으로 일관, 12px 일부만 통일 검토 |
| MainSection 타이포 | ⚠️ | 날짜·커플 이름 강조 클래스 적용 권장 |
| Toast vs alert | ⚠️ | 카카오 공유 실패만 alert 남음 → Toast 전환 권장 |
| console.log | ⚠️ | 프로덕션 제거/조건부 권장 |

**다음 단계 제안**
1. **즉시 적용 권장**: MainSection 날짜·커플 이름에 `text-heading-medium` / `text-heading-large` 적용.
2. **선택**: border-radius 통일(btn-rsvp, message-relationship, account-item), 카카오 alert→Toast, console 정리.
3. **추가 개선**: 모달 Escape/포커스 트랩, PWA·다국어 등은 기존 PROJECT_REVIEW 제안대로 필요 시 진행.

이 문서는 2026-02-18 전체 검토·디자인 확인 결과를 정리한 것입니다.

---

## 8. 인트로 배경 동영상 교체 (같은 날 구현)

### 요구사항
- 인트로의 배경 이미지를 `Wedding_video1.mp4` 동영상으로 교체
- 동영상: 반복 재생(loop), 기본 소리 on(브라우저 정책상 초기 muted 후 버튼으로 해제)
- 상단 fixed 음소거 버튼으로 소리 on/off 토글

### 구현 내용
- **IntroSection.jsx**
  - 배경 이미지용 `.intro-background` div 제거, `<video>` 요소 추가
  - `src="/images/Wedding_video1.mp4"`, `autoPlay`, `loop`, `playsInline`, `muted`(초기)
  - state: `isMuted`(초기 true → 자동재생 허용), 클릭 시 토글. `useEffect`로 `video.muted` 동기화
  - `IntersectionObserver`로 인트로 섹션이 보일 때만 상단 음소거 버튼 표시
  - react-icons: `PiSpeakerSlash`(음소거 시), `PiSpeakerHigh`(소리 on 시). `aria-label`로 접근성
- **IntroSection.css**
  - `.intro-background` → `.intro-video-wrap` + `.intro-video`로 변경. 비디오는 `object-fit: cover`로 영역 채움
  - `.intro-mute-btn`: `position: fixed`, `top: 16px`, `right: 16px`, `z-index: 100`, 원형 버튼 스타일

### 비디오 파일 위치
- 사용 경로: `/images/Wedding_video1.mp4`
- Vite는 `public/` 폴더를 루트로 서빙하므로 **`public/images/Wedding_video1.mp4`** 에 파일이 있어야 함.
- 프로젝트 루트의 `images/Wedding_video1.mp4`만 있는 경우 → `public/images/` 폴더를 만들고 해당 파일을 복사해 두면 됨.

---

## 9. 모달/토글 개선 작업 (같은 날 구현)

### 요구사항
1. **MainSection 연락하기**: 모달 → 하단 토글 패널로 변경
2. **MessageBoard 메시지 작성하기**: 모달 → 하단 토글 패널로 변경
3. **AccountSection 신랑측/신부측**: 토글 디자인을 버튼 디자인으로 변경

### 구현 내용

#### 1. MainSection 연락하기 - 하단 토글 패널
- **MainSection.jsx**
  - 모달 오버레이(`contact-modal-overlay`) 제거
  - 하단 고정 패널(`contact-toggle-panel`)로 변경
  - 패널은 `transform: translateY(100%)`로 숨김 상태, `open` 클래스 시 `translateY(0)`로 슬라이드 업
  - 외부 클릭 시 닫기 기능 추가 (`useEffect`로 이벤트 리스너 관리)
- **MainSection.css**
  - `.contact-toggle-panel`: `position: fixed`, `bottom: 0`, `border-radius: 24px 24px 0 0`
  - `.contact-toggle-header`: `position: sticky`, `top: 0`로 스크롤 시 헤더 고정
  - `.contact-toggle-close`: 닫기 버튼 스타일
  - 모바일 대응: `max-height: 90vh`, 패딩 조정

#### 2. MessageBoard 메시지 작성하기 - 하단 토글 패널
- **MessageBoard.jsx**
  - 모달 오버레이(`message-modal-overlay`) 제거
  - 하단 고정 패널(`message-toggle-panel`)로 변경
  - 동일한 슬라이드 업 애니메이션 및 외부 클릭 닫기 기능
- **MessageBoard.css**
  - `.message-toggle-panel`: MainSection과 동일한 구조
  - `.message-toggle-header`, `.message-toggle-close`: 동일한 스타일 패턴
  - 폼 스타일은 기존 유지, 패널 내부에서 자연스럽게 표시

#### 3. AccountSection 신랑측/신부측 - 버튼 디자인
- **AccountSection.jsx**
  - 토글 컨테이너에서 `is-groom`/`is-bride` 클래스 제거 (더 이상 슬라이더 배경 불필요)
- **AccountSection.css**
  - `.account-toggle`: 세그먼트 슬라이더 스타일 제거, 일반 버튼 그룹으로 변경
  - `.toggle-btn`: 
    - 기본: `background: white`, `border: 2px solid var(--bg-ivory)`, `border-radius: 12px`
    - 활성: `background: var(--point-pink)`, `color: white`, `border-color: var(--point-pink)`
    - 호버: `background: var(--point-pink-light)`, `transform: translateY(-2px)`, 그림자 효과
  - 슬라이더 배경(`::before`) 제거

### 디자인 일관성
- 하단 토글 패널: 둥근 상단 모서리(`24px`), 그림자, 슬라이드 업 애니메이션(`0.3s ease-out`)
- 버튼 스타일: AccountSection 토글 버튼은 다른 버튼들과 일관된 스타일 (border-radius, 그림자, 호버 효과)
- 접근성: `aria-label`, `role="tab"`, `aria-selected` 속성 유지

### 사용자 경험 개선
- 모바일 친화적: 하단 패널은 모바일에서 자연스러운 제스처와 일치
- 시각적 피드백: 버튼 호버/활성 상태 명확히 구분
- 스크롤 대응: 패널 내부 스크롤 가능, 헤더는 sticky로 고정

---

## 10. 코드 정리 및 공통화 작업 (같은 날 구현)

### 요구사항
- 한 곳 수정 시 다른 곳에 문제가 생기는 상황 방지를 위한 전반적인 코드 정리
- 미사용 코드 삭제 → 공통화 순서로 진행
- 기존 기능은 유지하면서 내부 구조만 개선

### 1단계: 미사용 코드 삭제

#### 삭제된 컴포넌트/파일
- **Navigation.jsx, Navigation.css**: App.jsx에서 주석 처리되어 사용되지 않음
- **celebrationSection.jsx**: App.jsx에 import/렌더링 없음
- **ContactParents.jsx, ContactParents.css**: 어디에서도 import되지 않음 (MainSection이 직접 구현)
- **MessageCard.jsx, MessageCard.css**: MessageBoard가 `<div className="message-card">` 직접 사용, 컴포넌트 미사용
- **AccountCard.jsx, AccountCard.css**: celebrationSection에서만 사용되는데 celebrationSection 자체가 미사용
- **Button.jsx, Button.css, ContactButton.jsx, ContactButton.css**: 사용처가 모두 미사용 컴포넌트뿐
- **buttons/index.js, cards/index.js**: export할 컴포넌트가 없어져서 불필요
- **styles/colors.css, styles/fonts.css**: 어디에서도 import되지 않음
- **MainSection.jsx**: `VENUE` destructuring 제거 (사용하지 않음)

### 2단계: 공통 유틸/훅 생성

#### 생성된 파일
- **`src/utils/formatPhone.js`**: `formatPhoneForLink` 함수 (전화번호 하이픈 제거)
- **`src/hooks/useLockBodyScroll.js`**: 모달/패널 열림 시 body 스크롤 잠금 훅

### 3단계: 공통 컴포넌트/훅 생성

#### 생성된 파일
- **`src/hooks/useBottomPanel.js`**: 하단 패널 관리 훅
  - body 스크롤 잠금 (`useLockBodyScroll` 사용)
  - 외부 클릭 시 닫기 처리
  - 패널 셀렉터와 트리거 버튼 셀렉터를 파라미터로 받아 재사용 가능
- **`src/components/common/BottomSheet.jsx`**: 하단 토글 패널 공통 컴포넌트
  - `open`, `onClose`, `title`, `children` props
  - 공통 스타일 적용
- **`src/components/common/BottomSheet.css`**: 하단 패널 공통 스타일
  - 기존 `.contact-toggle-panel`, `.message-toggle-panel` 스타일을 `.bottom-sheet-panel`로 통합
  - `pointer-events` 처리 포함

### 4단계: 공통화 적용

#### MainSection.jsx
- 기존 `useEffect` (body overflow, 외부 클릭) 제거
- `useBottomPanel` 훅 사용
- `BottomSheet` 컴포넌트로 패널 구조 교체
- `formatPhoneForLink` 유틸 import 사용

#### MessageBoard.jsx
- 기존 `useEffect` (body overflow, 외부 클릭) 제거
- `useBottomPanel` 훅 사용
- `BottomSheet` 컴포넌트로 패널 구조 교체

#### RSVPModal.jsx
- 기존 body overflow `useEffect` 제거
- `useLockBodyScroll` 훅 사용

### 결과
- **중복 코드 제거**: 연락하기 패널과 메시지 작성 패널의 동일한 로직을 공통 컴포넌트/훅으로 통합
- **유지보수성 향상**: 패널 관련 수정 시 한 곳만 수정하면 모든 패널에 적용
- **코드 일관성**: 동일한 패턴을 사용하여 예측 가능한 동작
- **기존 기능 유지**: 사용자가 보는 화면과 동작은 변경 없음

### 참고사항
- 기존 CSS 파일(`MainSection.css`, `MessageBoard.css`)의 패널 스타일은 클래스명이 달라 충돌 없음
- 필요 시 나중에 기존 패널 스타일 제거 가능 (현재는 유지)
